# Home Assist Agent 开发约束

## 产品定位

- 产品名：Home Assist Agent。
- 产品方向：连接自然语言、家庭上下文和多类设备来源的家庭生活助理。
- 重点用户：希望统一控制设备、管理提醒和个人习惯的家庭用户；先实现单家庭、单用户和本地部署，再扩展多用户、多入口和主动服务。
- 产品边界：Codex 负责理解和规划，本项目负责设备接入、安全执行、记忆、通知和审计。
- 产品底线：模型不得擅自推断或扩大高风险操作，但用户明确指定的操作应被准确执行。

## 核心原则

- 尊重明确指令：用户明确指定高风险设备、目标和动作时直接执行，不要求二次确认；信息不明确或由模型推断时必须澄清或拒绝。
- 统一设备接入：支持 Home Assistant、摄像头直连等设备来源；每个设备以其接入来源为事实源，通过事件监听实时更新、定时同步校正，请求不在执行前强制查询设备源。
- 推理与执行分权：Codex 可以理解和规划，但不能绕过权限、工具约束和执行策略。
- 隐私隔离：数据默认本地保存，仅按任务需要对外发送；不可信内容不能成为操作指令。
- 透明可审计：动作原因、目标和结果必须可解释；`message_id` 贯穿完整链路，审计失败时阻断外部副作用，凭据不得入库。

## 全链路审计

- 每个用户消息或系统触发事件必须具有唯一 `message_id`；该 ID 必须贯穿用户输入与输出、Codex 调用、外部服务调用以及错误分支。兼容字段 `request_id` 必须与 `message_id` 保持相同值。
- 必须完整记录用户发送的消息和最终返回给用户的内容。
- 必须完整记录发送给 Codex 的 prompt、参数，以及 Codex 返回的结构化内容、标准输出、标准错误和失败信息。
- 必须完整记录发送给 MCP、HTTP、消息平台或其他外部服务的业务请求，以及对方返回的业务响应和失败信息。
- 所有审计事件必须通过共享 `AuditRecorder` 追加写入，不得更新或删除历史事件；必须支持按 `message_id` 按序查询完整链路。
- Authorization、Token、API Key、Cookie、密码、客户端密钥和其他凭据不得写入审计库，必须在持久化前统一脱敏。
- 检查登录或认证页面时，不得读取完整 DOM 快照或表单状态；必须使用显式排除密码、Token、Cookie 和密钥字段的定向只读查询，且不得在输出或持久化记录中复述凭据。
- 外部副作用调用必须先成功写入请求审计；审计不可用时必须阻断调用，不能绕过或降级为无审计执行。
- 新增 LLM、MCP、HTTP、消息适配器、任务执行器或其他类似模块时，必须同时接入相同的 `message_id` 和请求/响应审计，并增加成功、失败、脱敏及链路完整性测试。

## 高风险操作确认边界

- 用户在设备页面主动点击或明确发起的人工交互操作，即使目标是门锁等高风险设备，也必须直接执行，不增加二次确认。
- 仅当 AI 对模糊指令作出自主决策，或 AI 基于事件自主判断并准备执行高风险操作时，才要求二次确认。
- 任何界面、策略、测试或文档都不得把“设备风险高”本身作为人工操作需要二次确认的充分条件。
